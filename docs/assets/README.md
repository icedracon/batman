# Demo assets

This directory stores lightweight reviewer-facing media used by the project README.

- `batman-demo.svg` shows the private-devnet BAL workflow.
- `social-preview.png` is ready for GitHub's social preview uploader.
- `social-preview.svg` is the editable source artwork.
